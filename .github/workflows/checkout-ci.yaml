# CI for Checkout Service

name: checkout-ci

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: Setup Go 1.22
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Build
        run: |
          cd src/checkout
          go mod download
          go build -o checkout-service main.go

      - name: Unit Tests
        run: |
          cd src/checkout
          go test ./...

  code-quality:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: Setup Go 1.22
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: v1.55.2
          working-directory: src/checkout
          args: --disable=unused,staticcheck,gosimple

  docker:
    runs-on: ubuntu-latest

    needs:
      - build
      - code-quality

    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v6
        with:
          context: src/checkout
          file: src/checkout/Dockerfile
          push: true
          tags: 281145281565.dkr.ecr.eu-west-3.amazonaws.com/opentelemetry/checkout:${{ github.run_id }}

  updatek8s:
    runs-on: ubuntu-latest

    needs: docker

    steps:
      - name: checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update tag in kubernetes deployment manifest
        run: |
          sed -i "s|image: .*|image: 281145281565.dkr.ecr.eu-west-3.amazonaws.com/opentelemetry/checkout:${{ github.run_id }}|" kubernetes/checkout/deploy.yaml

      - name: Commit and push changes
        run: |
          git config --global user.email "devops@users.noreply.github.com"
          git config --global user.name "Jawher Amiri"
          git add kubernetes/checkout/deploy.yaml
          git commit -m "[CI]: Update checkout image tag" || echo "No changes to commit"
          git push origin HEAD:main -f
